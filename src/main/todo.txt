1) ExperimentSequence generates log file within output directory which logs what time the experiments started and completed
2) ExperimentSequence saves results in directory structure mirroring the input
