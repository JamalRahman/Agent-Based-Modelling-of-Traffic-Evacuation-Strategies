1) ExperimentSequence generates directories if missing
2) Use of File.separatorChar instead of '/'
3) ExperimentSequence generates log file within directory which logs what time the experiments started and completed
