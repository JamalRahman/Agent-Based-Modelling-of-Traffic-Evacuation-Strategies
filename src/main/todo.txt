3) ExperimentSequence generates log file within output directory which logs what time the experiments started and completed
4) ExperimentSequence input can be a directory, if so it will run experiments for all files within
